docker network create ts-net

docker run -d --name timescaledb --network ts-net -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=circular_backend -p 5432:5432 timescale/timescaledb:latest-pg15

docker run -d --name pgadmin --network ts-net -e PGADMIN_DEFAULT_EMAIL=admin@admin.com -e PGADMIN_DEFAULT_PASSWORD=admin -p 5050:80 dpage/pgadmin4:latest

psql -h localhost -U admin

